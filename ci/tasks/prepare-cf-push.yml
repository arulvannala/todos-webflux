---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: openjdk
    tag: '8-jdk-alpine'

params:
  - GROUP_ID:
  - ARTIFACT_ID:
  - ROUTE:

inputs:
  - name: code-repo
  - name: version

outputs:
  - name: deployment

run:
  path: code-repo/ci/tasks/prepare-cf-push.sh
