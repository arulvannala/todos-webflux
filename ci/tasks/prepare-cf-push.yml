---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: openjdk
    tag: '8-jdk-alpine'

params:
  - GROUP_ID:
  - ARTIFACT_ID:
  - ROUTE:

inputs:
  - name: ci-scripts
  - name: code-repo
  - name: version

outputs:
  - name: deployment

run:
  path: ci-scripts/ci/tasks/prepare-cf-push.sh
